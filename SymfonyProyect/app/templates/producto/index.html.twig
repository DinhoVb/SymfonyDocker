{% extends 'base.html.twig' %}

{% block title %}productos{% endblock %}

{% block body %}
<style>
    .example-wrapper {
        margin: 1em auto;
        max-width: 800px;
        width: 95%;
        font: 18px/1.5 sans-serif;
    }

    .example-wrapper code {
        background: #F5F5F5;
        padding: 2px 6px;
    }
</style>
    <section class="myform-area">
        <div class="container">
            <div class="row justify-content-center">
                <div class="col-lg-8">
                    <div class="form-area login-form">
                        <div class="form-content">
                            <h2>Lista de Productos</h2>
                            <p>Si desea ver el listado de todos los productos, le puede dar al clic al boton y esta lo
                                redireccionará</p>
                            <div class="justify-content-center">
                                <a href="{{ path('list_producto') }}" class="btn btn-warning float-center">Mostrar Lista</a>
                            </div>
                        </div>
                        <div class="form-input">
                            <h2>Filtro de Producto</h2>
                            <form method="post", action="{{ path('filtrar_producto') }}">
                                <div class="form-group">
                                    <input type="text" id="inputName" name="nombre" required>
                                    <label for="inputName">Nombre</label>
                                </div>
                                <div class="form-group">
                                    <input type="number" id="inputLimit" name="limit" required>
                                    <label for="inputLimit">Limite Maximo</label>
                                </div>
                                <div class="myform-button">
                                    <button class="myform-btn" type="submit">buscar</button>
                                </div>
                            </form>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </section>


{% endblock %}